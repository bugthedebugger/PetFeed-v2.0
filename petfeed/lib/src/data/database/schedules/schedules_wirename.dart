/// schedules table name
final tableSchedules = 'schedules';

/// primary key
final columnId = 'id';

/// amount
final columnAmount = 'amount';

/// day
final columnDay = 'day';

/// feedTime
final columnFeedTime = 'feedTime';

/// groupId
final columnGroupId = 'groupId';

/// petId
final columnPetId = 'petId';

/// uId
final columnUID = 'uId';

/// schedules table columns list
final schedulesColumns = [
  columnId,
  columnAmount,
  columnDay,
  columnFeedTime,
  columnGroupId,
  columnPetId,
  columnUID,
];
